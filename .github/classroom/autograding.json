{
	"tests": [
		{
		"name": "Make Vanilla",
		"setup": "",
		"run": "make clean Vanilla",
		"timeout": 10,
		"points": 50
	},
		{
			"name": "Memory leak check",
			"setup": "sudo apt-get update -y && sudo apt-get install -y valgrind && make all",
			"run" : "valgrind --leak-check-full --error-exitcode=1 Vanilla",
			"input": "",
			"output": "",
			"comparison":"included",
			"timeout": 10,
			"points": 50
		}
	]
}
